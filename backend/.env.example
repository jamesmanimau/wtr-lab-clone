# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=wtrlab
DB_PASSWORD=wtrlab_secret
DB_NAME=wtrlab
DB_SSLMODE=disable

# JWT
JWT_SECRET=change-this-to-a-random-secret

# Server
SERVER_PORT=8080

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Cookie Secure (set false for dev without HTTPS)
COOKIE_SECURE=true
